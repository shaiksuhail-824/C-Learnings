# include <stdio.h>
//A program to check if the given number is a natural number.
int main() {
    int num1;
    scanf("%d",&num1);
    if(num1>0){
        printf("It is natural number");
    }else{
        printf("It is not natural number");
    }

   return 0;
}